import { AuthStagePlaceholder } from "@/components/auth/auth-stage-placeholder";

export default function RegisterConfirmPage() {
  return (
    <AuthStagePlaceholder
      eyebrow="Email confirmation"
      title="Token-driven routes are scaffolded."
      description="Register confirmation remains a placeholder in Stage 1 so the route structure exists without prematurely implementing backend flow handling."
    />
  );
}
