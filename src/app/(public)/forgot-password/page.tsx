import { AuthStagePlaceholder } from "@/components/auth/auth-stage-placeholder";

export default function ForgotPasswordPage() {
  return (
    <AuthStagePlaceholder
      eyebrow="Recovery"
      title="Forgot password foundation is in place."
      description="The responsive auth shell and shared card primitives are ready. Generic success handling and actual API integration will be added in Stage 2."
    />
  );
}
